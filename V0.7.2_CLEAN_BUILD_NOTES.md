> Historical baseline report. For the current pilot, read V0.8.0_RELEASE_REVIEW.md.

# v0.7.2 clean-build review pilot

This revision repairs the incomplete v0.7.1 Windows build package and adds the Settings > About presentation requested for review.

## Corrected

- Restored `build-requirements.txt`, `requirements.txt`, and the Windows review README to the package root.
- Added a build preflight that lists every missing required file and stops before downloading tools or changing an existing build output.
- Aligned application, Python package, dashboard lockfile, build folder, and documentation to `0.7.2-demo`.
- Preserved the prior security boundary: no bundled credentials, email sends disabled, Dummy writes disabled by default, and live Tracker writes blocked.

## Settings > About

- Settings is now visible to every pilot role.
- About is visually separated from administrative controls and opens by default.
- The page displays the official Macrotech artwork, product identity, exact version, pilot classification, workspace role, data mode, safety boundary, and support-reporting guidance.
- Administrative controls remain unavailable to employees. CEO-only audit data remains restricted by the existing bridge projection.

## Validation completed in the Linux audit workspace

- React and TypeScript production build passed.
- 68 isolated backend/package tests passed.
- Package-completeness and no-bundled-credential regression tests passed.
- No Google Sheet, Gmail, OAuth, Firebase Hosting, Cloud Run, or production action was performed.

The actual Windows executable still must be built and tested on Windows. This ZIP is not a signed production installer.
